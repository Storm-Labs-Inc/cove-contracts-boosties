{
  "name": "smart-contracts-core",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "husky install",
    "clean": "rm -rf cache out",
    "build": "forge-deploy gen-deployer && forge build --sizes",
    "test": "forge test -vvv --gas-report",
    "coverage": "forge coverage --report lcov",
    "slither": "forge clean && forge build --skip test script --build-info && slither . --ignore-compile",
    "deployLocal": "DEPLOYMENT_CONTEXT=1-fork forge script script/Deployments.s.sol --rpc-url http://localhost:8545 --broadcast --private-key ac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80 -vvv && forge-deploy sync;",
    "lint": "pnpm run lint:sol && pnpm run prettier:check",
    "lint:fix": "pnpm lint:sol:fix && pnpm run prettier:write",
    "lint:sol": "forge fmt --check && npx solhint \"{script,src}/**/*.sol\" --ignore-path .solhintignore && npx solhint \"{test}/**/*.sol\" --config .solhint.test.json --ignore-path .solhintignore",
    "lint:sol:fix": "forge fmt && npx solhint --fix \"{script,src}/**/*.sol\" --ignore-path .solhintignore && npx solhint --fix \"{test}/**/*.sol\" --config .solhint.test.json --ignore-path .solhintignore",
    "prettier:check": "npx prettier --check \"**/*.{json,md,yml,yaml}\"",
    "prettier:write": "npx prettier --write \"**/*.{json,md,yml,yaml}\""
  },
  "lint-staged": {
    "{script,src}/**/*.sol": [
      "forge fmt",
      "npx solhint --fix --noPrompt --config .solhint.json --ignore-path .solhintignore"
    ],
    "test/**/*.sol": [
      "forge fmt",
      "npx solhint --fix --noPrompt --config .solhint.test.json --ignore-path .solhintignore"
    ],
    "**/*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^17.8.0",
    "@commitlint/config-conventional": "^17.8.0",
    "@types/node": "^20.8.7",
    "forge-deploy": "^0.2.0",
    "husky": "^8.0.3",
    "lint-staged": "^15.0.1",
    "prettier": "^3.0.3",
    "solhint": "^4.0.0"
  }
}
